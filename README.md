# Document-Scanner
A Python-based document scanner that uses OpenCV to detect document boundaries in an image and applies a 4-point perspective transform to provide a clean, top-down, "scanned" result with adaptive thresholding.
